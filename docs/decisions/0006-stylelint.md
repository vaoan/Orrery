# ADR 0006 — stylelint reconciliation

**Date:** 2026-09-11
**Status:** accepted
**Spec:** docs/specs/2026-09-08-phase-2b-reconciliation-design.md
**Generated by:** `orrery reconcile` from aeleos `5e70a21` and libra `1c79de72`. Do not edit by hand; re-run and re-commit.

Ruling order: stricter wins; if strictness is undefined, the more consistent option; then the option that benefits the code most. Project data is a parameter. `a` is aeleos, `b` is libra.

| test | rows |
|---|---|
| agree | 2 |
| adopt | 2 |
| strictest | 3 |
| consistency | 0 |
| benefit | 1 |
| parameter | 0 |
| inert | 0 |
| residue | 0 |

## *

| key | aeleos | libra | ruling | test | tier | note |
|---|---|---|---|---|---|---|
| `extends` | `["stylelint-config-standard","stylelint-config-tailwindcss"]` | `["stylelint-config-standard","stylelint-config-tailwindcss"]` | `["stylelint-config-standard","stylelint-config-tailwindcss"]` | agree | class | more presets check more |
| `rules.at-rule-no-unknown` | `[true,{"ignoreAtRules":["apply","layer","tailwind","variants","responsive","screen","keyframes","font-face","custom-variant","theme","utility"]}]` | `[true,{"ignoreAtRules":["apply","layer","tailwind","variants","responsive","screen","keyframes","font-face","custom-variant","theme"]}]` | `[true,{"ignoreAtRules":["apply","custom-variant","font-face","keyframes","layer","responsive","screen","tailwind","theme","utility","variants"]}]` | benefit | class | union of Tailwind at-rules: every one listed exists in the framework and must parse |
| `rules.custom-property-pattern` | `["^(--)?[a-z0-9][a-z0-9-]*(--[a-z0-9-]+)?$",{"message":"Expected custom property name to be kebab-case; Tailwind theme line-height tokens may include a double dash."}]` | `["^(--)?[a-z0-9][a-z0-9-]*(--[a-z0-9-]+)?$",{"message":"Expected custom property name to be kebab-case; Tailwind theme line-height tokens may include a double dash."}]` | `["^(--)?[a-z0-9][a-z0-9-]*(--[a-z0-9-]+)?$",{"message":"Expected custom property name to be kebab-case; Tailwind theme line-height tokens may include a double dash."}]` | agree | class |  |
| `rules.declaration-block-no-duplicate-properties` | `[true,{"ignore":["consecutive-duplicates-with-different-syntaxes"]}]` | — | `[true,{"ignore":["consecutive-duplicates-with-different-syntaxes"]}]` | adopt | class |  |
| `rules.no-descending-specificity` | — | — | `{"$inherit":true}` | strictest | class | a disabled a preset rule; the preset's default (on) is stricter, so the bundle omits the key and the preset applies |
| `rules.no-duplicate-selectors` | — | — | `{"$inherit":true}` | strictest | class | a disabled a preset rule; the preset's default (on) is stricter, so the bundle omits the key and the preset applies |
| `rules.property-no-vendor-prefix` | — | — | `{"$inherit":true}` | strictest | class | a disabled a preset rule; the preset's default (on) is stricter, so the bundle omits the key and the preset applies |
| `rules.selector-attribute-name-disallowed-list` | `[["class"],{"message":"Do not select the class attribute — that reaches into Tailwind's generated output, which cannot see what the element asked for. Give the element a class of your own (see @utility surface) and style that."}]` | — | `[["class"],{"message":"Do not select the class attribute — that reaches into Tailwind's generated output, which cannot see what the element asked for. Give the element a class of your own (see @utility surface) and style that."}]` | adopt | class |  |

